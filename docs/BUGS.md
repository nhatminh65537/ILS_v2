# BUGS.md — ILS v2 Known Bugs & Issues

> Track all known bugs here. Update when fixing or discovering new issues.
> Format: one table per severity level. Move bugs to **Fixed** section after resolving.

---

## Active Bugs

### High — Breaks functionality

| # | File | Description | Fix |
|---|------|-------------|-----|
| - | - | No active high bugs currently tracked. | - |

### Medium — Degrades functionality

| # | File | Description | Fix |
|---|------|-------------|-----|
| - | - | No active medium bugs currently tracked. | - |


### Low — Minor issues / tech debt

| # | File | Description | Fix |
|---|------|-------------|-----|
| L1 | `ai/services/llm_client.py` | LLM client is a mock — always returns a hardcoded string. | Implement real provider call in Slice 10 (deferred). |
| L2 | `api/models.py` | `QuizQuestion` thiếu composite index trên `(quiz, status)` — field `status` được filter thường xuyên nhưng chỉ có index trên `quiz` đơn lẻ. | Thêm `models.Index(fields=['quiz', 'status'])` trong Meta. |
| L4 | Frontend testing environment (Playwright integrated browser viewport) | Case responsive J-3 (`<=768px`) chưa xác thực được trong phiên tool hiện tại vì viewport thực tế bị giữ ~804px; chưa kết luận pass/fail. | **Deferred test note:** chạy lại bằng Playwright CLI headless/headed ngoài integrated browser để kiểm tra breakpoint chuẩn. |
| - | - | (L5 fixed — see F36 below.) | - |

---

## Recently Fixed Bugs (last 10)

> Older fixes archived — see "Fixed (Archived)" below + git log for full history.

| # | Fixed | File | Description | How Fixed |
|---|-------|------|-------------|-----------|
| F44 | 2026-06-03 | `backend/api/services/course_service.py`, `backend/api/views/courses.py` (verified, +tests) | **[_-13] Catalog filter quá đơn giản, FE-only**: `/vi/courses` lọc client-side toàn bộ; BE `filter_visible_learn_courses` chưa hỗ trợ tag AND-filter, list endpoint dùng `PageNumberPagination` nhưng FE gửi `limit/offset` → phân trang không thực sự chạy. | (BE) Thêm tag AND-filter: `?tags=1,2,3` → mỗi tag id `.filter(tag_mappings__tag_id=id)` chồng nhau (AND) + `.distinct()` (helper `_parse_tag_ids`). Đổi `LearnCourseViewSet.pagination_class = LimitOffsetPagination` để FE `limit/offset` lái paging trực tiếp (trả `count/next/previous`). (FE) `CourseCatalogClient` lọc qua BE: state filter + nút **Lọc/Đặt lại** → gọi BE; giữ grid mounted + overlay `opacity-60` khi loading (không nháy); phân trang prev/next; status dropdown "Tất cả" + 3 trạng thái. Filter universe (category/tag chips) nạp từ `/api/learn/{categories,tags}`. |
| F43 | 2026-06-03 | `backend/api/views/courses.py`, `backend/api/services/course_service.py`, `backend/api/serializers/course.py`, `backend/api/urls.py`, `frontend/src/components/features/courses/admin/{AdminLearnTreeTab,AdminLearnNodeRow,AdminLearnNodeTree,AdminLearnNodeCreateDialog,AdminLearnNodeRenameDialog}.tsx`, `frontend/src/hooks/useAdminLearnCourseTree.ts` (verified, +tests, +@dnd-kit) | **[D-04]/[_-06]/[_-07]/[_-08]/[_-09]/[_-10] Reorder/move cây hỏng + UX tệ**: reorder cũ patch `position±1` trên 1 node, BE `update()` set thẳng position không shift sibling → position trùng → thứ tự loạn (gốc D-04); move qua `window.prompt` nhập parent id; form tạo node inline thiếu point/time; sort không ưu tiên folder; cây không hiện order/point/time. | (BE) Action mới `POST .../nodes/reorder/` nhận `{parent_id, ordered_ids}` → `reorder_course_node_siblings` reindex `position=0..n` theo mảng (idempotent, không trùng); list/children `order_by('is_item','position','id')` (folder-first); `create_course_node_atomic` tự gán position = max(sibling)+1. (FE) Redesign cây bằng **@dnd-kit**: drag handle reorder trong cùng parent (gửi `ordered_ids`), thả vào folder khác → move; `NodeCreateDialog` riêng (folder/lesson + point/time + MD template mặc định + 1 ô Tiêu đề dùng chung) mở từ nút "+ Thêm" trên folder/root; node row hiện order/point/time; rename qua dialog. |
| F42 | 2026-06-03 | `frontend/src/components/ui/confirm-dialog.tsx` (new), `frontend/src/components/features/courses/admin/{AdminLearnCourseListPageClient,AdminLearnLessonEditorPageClient,AdminLearnNodeRow,AdminLearnLessonMetadataTab}.tsx`, `frontend/src/hooks/{useAdminLearnCourses,useAdminLearnCourseTree,useAdminLearnLessonEditor}.ts`, `frontend/src/services/courses.service.ts`, `backend/api/{views/courses.py,serializers/course.py}` (verified, +test) | **[_-01]/[_-02]/[_-03]/[_-04]/[_-11]/[_-12] Learn admin UX**: dùng `window.confirm/prompt` của trình duyệt; không có xóa hoàn toàn course (DELETE không truyền `mode` → BE default archive); tạo lesson trong folder đang expand → folder rỗng (refreshTree xóa childrenByParentId nhưng giữ expandedNodeIds, không reload child); node title vs lesson title không sync khi rename; không có UI sửa metadata lesson; lesson editor không có link về cây edit của course. | (FE) `ConfirmDialog` dùng chung (dựng trên `ui/dialog`) thay mọi native dialog; course list có **Lưu trữ** (archive) + **Xóa vĩnh viễn** (purge, ConfirmDialog destructive yêu cầu gõ slug) — service truyền `?mode=archive\|purge`; `refreshTree` reload lại children mọi folder đang expand (fix folder rỗng); thêm tab **Thông tin** (Metadata) sửa title/point/time/video_duration + breadcrumb về `/admin/learn/courses/{slug}`. (BE) rename node-item đồng bộ `lesson.title`; `LearnLessonDetailSerializer` thêm `course_slug`/`course_title` cho breadcrumb. |
| F41 | 2026-06-01 | `backend/api/serializers/course.py`, `frontend/src/hooks/useAdminLearnCourseTree.ts`, `frontend/src/components/features/courses/admin/AdminLearnTreeTab.tsx` (verified) | **[D-03] node create 400 (markdown content rỗng)**: `POST /api/learn/courses/{slug}/nodes/` trả `400 58 byte` (`{"lesson":{"content_md":["This field may not be blank."]}}`) khi tạo lesson Markdown không có nội dung. FE (`AdminLearnTreeTab`) gửi `content_md: ""`; serializer field `content_md=CharField(allow_blank=False)` reject ngay ở field-level **trước** khi `validate()` (có message thân thiện "Required for markdown lessons") chạy. Lỗi bị che bởi i18n F40 nên user chỉ thấy MISSING_MESSAGE. | (BE) `content_md`/`video_url` → `allow_blank=True` để `validate()` cross-field emit message thân thiện (vẫn reject rỗng vì `validate()` strip + check `not content_md`). (FE) Thêm guard trong `submitCreateLessonNode`: Markdown content rỗng → `errors.markdownContentRequired`, Video url rỗng → `errors.videoUrlRequired` (đồng nhất với `useAdminLearnLessonEditor`). |
| F40 | 2026-06-01 | `frontend/src/lib/learn-admin-error-map.ts`, `frontend/src/hooks/useAdminLearn{Courses,CourseTree,LessonEditor}.ts` (verified) | **[D-02]/[D-03] i18n double-namespace (class F29/F30/F39)**: `MISSING_MESSAGE: adminLearn.adminLearn.errors.{slugConflict,createLessonNodeFailed,...}`. `mapLearnAdminErrorToMessageKey` + fallbackKey ở các hook trả key full-path (`adminLearn.errors.*`), nhưng mọi consumer render bằng `t(key)` với `t=useTranslations('adminLearn')` → next-intl ghép thành `adminLearn.adminLearn.errors.*`. Ảnh hưởng TẤT CẢ error display của learn admin (course/tree/lesson editor). Đồng thời `slugConflict` bị check sau `validation` (`required`/`invalid`) trong map. | Đổi tất cả key trả về + fallbackKey sang relative namespace (`errors.*`) — giữ `useTranslations('adminLearn')` + `t(key)` ở consumer (theo precedent F29). Reorder check `slug already exists` lên trước rule `validation`. |
| F39 | 2026-06-01 | `frontend/src/components/features/notifications/NotificationBell.tsx`, `frontend/messages/{vi,en}.json` (verified) | **[_-10] i18n double-namespace**: `MISSING_MESSAGE: notifications.notifications.errors.authRequired`. `useNotificationSocket` đặt `errorKey` = full path từ root (`notifications.errors.authRequired`), nhưng `NotificationBell` render bằng `t(socketErrorKey)` với `t=useTranslations('notifications')` → next-intl ghép thành `notifications.notifications.errors.*` → crash khi WS notification auth bị từ chối. Cùng class F29/F30/F33. | Thêm `tRoot = useTranslations()` (root scope) và resolve `socketErrorKey` bằng `tRoot(...)` thay vì `t(...)`. Key giữ nguyên full-path. |
| F38 | 2026-06-01 | `backend/auth_app/permissions.py`, `backend/tests/fixtures/auth.py`, `backend/api/tests/test_learn_{course,lesson}_api.py`, `backend/auth_app/tests/test_authz_bitmap.py` (new) | **[B-03]/[B-__] — RBAC bitmap-guard (CRITICAL)**: `HasJWTPermission` đọc claims bằng `request.auth if isinstance(request.auth, dict) else {}`. Nhưng `request.auth` là object `AccessToken` (SimpleJWT), KHÔNG phải `dict` → bitmap đọc thành `''` → **nhánh bitmap (production) bị bỏ qua hoàn toàn**, rơi xuống fallback role-name + `is_superuser` short-circuit. Hệ quả: mọi permission cấp qua **custom role** (vd role `Manager` mang `api.admin_user.list`) hoặc `system.*` đều vô tác dụng khi `authorization_enabled=true`; chỉ built-in role (qua role-name match) + superuser còn "hên-xui đúng". Bug ẩn vì mọi test dùng `force_authenticate` (auth=None) → luôn chạy fallback, chưa từng chạm nhánh bitmap. | (1) Đọc `request.auth.get('permissions')` (hỗ trợ `AccessToken` + dict). (2) **Xoá hoàn toàn fallback role-name + is_superuser short-circuit** → authz thuần bitmap, fail-closed (single source of truth). (3) Refactor test fixtures (`admin/editor/member_client`) sang **JWT thật** + gán Role thật cho fixture user → test bao phủ production path. (4) Thêm `test_authz_bitmap.py`: custom-role-with-permission → 200, thiếu perm → 403, superuser-no-role → 403. |
| F26 | 2026-04-19 | `backend/auth_app/services/token_service.py`, `frontend/src/components/layouts/AdminAccessGate.tsx`, `frontend/src/components/features/auth/AdminLoginForm.tsx`, `frontend/src/lib/rbac-claim.ts` | **H3 + H7**: frontend admin surface only checked authenticated state, allowing member accounts onto admin routes and destabilizing `/admin/users` verification. | Added `admin_surface` JWT claim derived from backend `Admin`/`Editor` role membership, wired the frontend admin guard to that claim, rejected non-admin-surface admin logins immediately, and aligned MSW mock tokens/messages with the same contract. |
| F27 | 2026-04-20 | `frontend/playwright.slice7.checklist.test.ts`, `frontend/scripts/slice7-diagnostics.mjs` | Slice 7 browser checklist helpers produced false negatives by opening protected quiz routes without logging in first and by using stale admin password `admin` instead of seeded `admin1234`. | Updated the checklist Playwright helpers to authenticate before protected user-route checks, wait for login redirects deterministically, and switched both checklist and diagnostics scripts to canonical seeded credentials (`admin1234`, `member1234`). |
| F28 | 2026-04-20 | `backend/api/services/quiz_service.py`, `backend/realtime/consumers/quiz_consumer.py`, `frontend/src/hooks/useQuizSession.ts` | Slice 7 runtime mismatches: default quiz config randomized questions/options despite checklist contract, empty quizzes errored instead of finishing `0/0`, and frontend WS client ignored `NEXT_PUBLIC_WS_URL` while collapsing protocol close codes into generic connection errors. | Changed quiz-config defaults to `random_question=false` / `random_option=false`, made empty published quizzes finish immediately over WS, updated `useQuizSession` to prefer `NEXT_PUBLIC_WS_URL`, and mapped auth/session-specific close codes to deterministic client error states. Regression tests added for config defaults and empty-quiz finish behavior. |
| F29 | 2026-04-20 | `frontend/src/hooks/useAdminNotifications.ts`, `frontend/src/components/features/notifications/AdminNotificationBroadcastClient.tsx` | Task 9.5 admin notifications rendered raw duplicated i18n keys (`adminNotifications.adminNotifications.errors.submitFailed`, `...historyLoadFailed`) because hook/component emitted fully-qualified keys while `useTranslations('adminNotifications')` already scoped namespace. | Changed admin notification error keys to relative namespace paths (`errors.*`) for both submit and history flows; UI now resolves localized messages correctly. |
| F30 | 2026-04-20 | `frontend/messages/en.json`, `frontend/messages/vi.json` | Admin notifications metadata placeholder triggered next-intl runtime parse error `INVALID_MESSAGE: MALFORMED_ARGUMENT` due to ICU-like braces in translation value (`{"key":"value"}`). | Replaced placeholder with non-ICU literal (`"key": "value"`) in both locales to avoid argument parsing. |
| F31 | 2026-04-20 | `backend/db.sqlite3`, `backend/api/migrations/0008_notification_event_key.py` | Admin broadcast endpoint returned `500` (`OperationalError: table notification has no column named event_key`) on local runtime because migration `api.0008_notification_event_key` existed but was not applied to active SQLite database. | Applied `../.venv/Scripts/python.exe manage.py migrate api`; verified `showmigrations` now marks `0008_notification_event_key` as applied. |
| F32 | 2026-05-04 | `frontend/app/[locale]/page.tsx` | Homepage always showed login/register CTA buttons regardless of authentication state (server component couldn't check auth). | Extracted CTA into client component `HomeCTA.tsx` using `useAuth()` hook; shows dashboard/courses links when authenticated. |
| F33 | 2026-05-04 | `frontend/messages/en.json`, `frontend/messages/vi.json` | `flagPlaceholder` (`ILS{...}`) and `flagValuePlaceholder` (`CTF{...}`) triggered `INVALID_MESSAGE: MALFORMED_ARGUMENT` next-intl parse error because unescaped braces `{...}` were interpreted as ICU message arguments. | Escaped braces with ICU single-quote literals: `ILS'{...}'` and `CTF'{...}'`. |
| F34 | 2026-05-04 | `frontend/src/hooks/useAdminChallenges.ts`, `frontend/src/components/features/challenges/admin/AdminChallengeInstancesPageClient.tsx` | `result.items` accessed on `PaginatedResponse<T>` which uses `results` field (DRF convention); `items` was `undefined`, causing `data` to be `undefined` and crashing on `.length` access. | Changed to `result.results` with proper type casting; added defensive null guard in `AdminChallengeListPageClient.tsx`. |
| F35 | 2026-05-04 | `frontend/src/stores/courses.store.ts`, `frontend/src/components/features/courses/LessonViewerClient.tsx` | Lesson complete button didn't stay disabled after re-visiting a completed lesson because `resetLessonState()` cleared `isCompleted` with no way to restore (no GET progress endpoint). | Added `completedLessonIds: Set<number>` to store; `resetLessonState(currentLessonId?)` preserves completion for known IDs; added early-return guards in `handleStart`/`handleComplete`. |
| F36 | 2026-05-28 | `backend/auth_app/permissions_registry.py` (new), `backend/auth_app/services/permission_discovery.py`, `backend/auth_app/services/token_service.py`, `backend/api/services/{course,challenge,quiz,lesson}_service.py`, 11 view files, `frontend/src/lib/rbac-claim.ts`, `frontend/src/lib/permission-error-bus.ts` (new), `frontend/src/components/feedback/PermissionErrorDialog.tsx` (new), `frontend/src/components/layouts/{AdminSidebar,AdminProtectedSectionGate}.tsx` (new), `frontend/src/components/layouts/AdminLayout.tsx`, MSW handlers | **L5 + cơ chế phân quyền mở rộng**: cơ chế cũ chỉ sinh permission từ URL scan; service-layer hardcode `role__name__in=['Admin','Editor']`; FE phải decode bitmap + catalog để gate buttons. | (BE) Thêm code-registered permission registry (`system.*` namespace) merge vào `discover_permissions()`; thêm 13 perm gồm `system.admin_portal.access`, `system.material.{read_draft,read_archive,purge}` + 9 admin section perms. Refactor 4 service `is_editor_or_admin()` → `_can_read_draft/_can_read_archive` qua `PermissionService.check_permission`. `TokenService` derive `admin_surface` + `admin_sections[]` claim từ permission. Thay tất cả `@add_role_granted('Admin', ...)` literal → `BUILTIN_ROLE_*` constants (133 chỗ, 11 file). (FE) Xóa `hasPermissionKey/canManage*` capability catalog. Sidebar admin filter theo `admin_sections`. `AdminProtectedSectionGate` redirect khi không có section. Axios 403 interceptor emit event → `PermissionErrorDialog` global. |
| F37 | 2026-05-29 | `backend/auth_app/{authentication.py(new),services/{token,session,sso}_service.py,services/session_service.py,views.py,serializers.py,constants.py,tests/test_auth_api.py}`, `backend/backend/settings.py`, `backend/api/models.py`, `backend/api/migrations/0010_auth_session_hardening.py` (new), `backend/realtime/consumers/{notification,quiz}_consumer.py`, `frontend/src/lib/{axios,auth-error-map}.ts`, `frontend/src/hooks/useAuth.ts`, `frontend/src/stores/auth.store.ts`, `frontend/src/components/{layouts/SessionNavControls.tsx,features/profile/AccountForm.tsx,features/dashboard/AdminPortalCard.tsx(new)}`, `frontend/app/[locale]/(app)/dashboard/page.tsx`, `frontend/messages/{vi,en}.json` | **Integration-test Pass 1 nhóm A — 8 bug auth/session phát hiện**: A-02 email trùng vẫn register OK + message lỗi field-level generic; A-05 rate-limit login dùng sliding window kéo dài 5 phút thay vì 1; **A-06 (CRITICAL)** JWT refresh race khi nhiều request 401 đồng thời (rotate xong → các request sau bị 401 → user bounce login); **A-08 (CRITICAL SECURITY)** logout-all chỉ revoke `UserSession` mà không invalidate JWT → tab/browser khác vẫn gọi API ~15 phút; `_-01`/`_-02` logout stuck/redirect dashboard do race giữa store-clear và `GuestOnlyGate`; `_-03` dashboard user hiển thị link admin portal cho mọi user. | (BE A-02) `User.email` partial-unique constraint + `validate_email` case-insensitive. (BE A-05) `cache.add` + `cache.incr` cho fixed window 5 phút. (BE A-06) `UserSession.replaced_by/replaced_at` + grace-window lookup 10s trong `refresh_tokens` → idempotent cho refresh đồng thời. (BE A-08) `RevocationCheckingJWTAuthentication` extends `JWTAuthentication`, check `UserSession.revoked_at` mỗi request (cache `session_active:{id}` TTL 30s, invalidate trong `SessionService` mọi đường revoke). Thêm claim `session_id` vào JWT (2-phase create session: row → sign → attach hash). WS consumers cũng check `session_id` revoked. (FE A-06) Promise-singleton `inflightRefresh` trong axios → 1 POST refresh duy nhất cho mọi 401 đồng thời. (FE _-01/_-02) `useAuth.logout` clear store sync TRƯỚC server call; `SessionNavControls` + `AccountForm` dùng `window.location.assign` (hard nav) tránh race với `GuestOnlyGate`. (FE A-08) `BroadcastChannel('ils-auth')` trong `clearAuth` → các tab khác cùng browser auto-logout instant; BE check covers cross-browser. (FE A-02-b) `auth-error-map.ts` thêm keyword `already exists`/`duplicate` → `auth.errors.{usernameTaken,emailTaken}` + i18n keys mới. (FE _-03) `AdminPortalCard` client component dùng `useSyncExternalStore` + store check `hasAdminSurfaceAccess(token)`, chỉ render khi đủ quyền. Update `test_token_refresh_old_token_invalid_after_rotation` cho contract grace-window mới (idempotent trong 10s, 401 sau). |

---

## Fixed (Archived)

> **F1–F25 (2026-03-09 → 2026-04-14):** 25 bugs đóng trong các batch backend refactor + frontend MSW/i18n + RBAC unification + AI app cleanup. Bao gồm: typos `ai/` package (F1–F6), settings INSTALLED_APPS (F7), admin gate / RBAC normalization (F8–F9), MSW/quiz handlers (F10–F11, F22–F24), i18n ICU placeholders (F12, F14), permission unification removing `IsAdminUser` + `action_permission_map` (F15–F17), quiz progress endpoint wiring (F18), admin user uniqueness (F19), public profile route + 404 contract (F20), account update UX (F21), settings enum validation (F25).
>
> **Để tra cứu chi tiết:** `git log -- docs/BUGS.md` (commits trước 2026-04-19) hoặc `docs/reports/2026-04-14_*.md` (3 reports gồm bugfix batch H4/H6/H8/M6/M7/M9/M10, H2/M2/M4/M5/L3, refactor closure).

---

## Tracking Notes

- **Discovery session:** 2026-03-09 — full project review identified F1–F7 architecture/typo issues.
- **Architecture violations to guard against:** See `docs/ARCHITECTURE.md` §7 "What NOT To Do".
- **Doc-code inconsistencies:** Resolved during normalization Session 2 (2026-05-04). Lesson `status`, `video_duration`, course `structure_version` are now in sync between `docs/DATA_MODEL.md` and `backend/api/models.py`. See `docs/normalization/02-tier2-drift.md` items D-02-01 / D-02-02 / D-02-03.
