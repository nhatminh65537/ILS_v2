# Session Report: Bugfix H2 M2 M4 M5 L3

**Date:** 2026-04-14
**Slices / Areas:** Slice 7 Quiz UI + Slice 8 Profile UI (bugfix pass)

## Summary

Implemented and verified a focused bugfix batch for five active FE/MSW issues: admin quiz status filter (H2), quiz session restart flow (M2), session dialog ICU interpolation (M4), account form save-button gating (M5), and quiz delete confirm interpolation (L3). Changes were constrained to frontend app, locale messages, and MSW handlers while preserving backend contracts.

## Completed Items

- [Fixed H2 by applying status-aware filtering in quiz list MSW handler before pagination]
- [Fixed M2 by adding deterministic session remount strategy using restart nonce query + keyed client render]
- [Fixed M4 by correcting ICU placeholder usage for session revoke dialog in vi/en locales]
- [Fixed M5 by introducing normalized hasChanges gating for account save action]
- [Fixed L3 by correcting ICU placeholder usage for quiz delete confirm in vi/en locales]
- [Updated docs/BUGS.md: moved H2, M2, M4, M5, L3 into Fixed history]
- [Validated frontend lint and production build after changes]

## Key Implementations

### H2 - Admin Quiz Status Filter (MSW)

1. Read `status` query parameter in quiz list mock handler.
2. Apply exact status filter on `quizzesFixture` when `status` is provided and not `all`.
3. Preserve full list behavior for missing status or `status=all`.
4. Run pagination on filtered dataset to keep list contract deterministic.

### M2 - Quiz Try Again Restart

1. Replace same-route push/refresh with route replacement containing `restart=<timestamp>` nonce.
2. Extend quiz session page to read `searchParams.restart`.
3. Key `QuizSessionClient` by `quizId + restart` to force component remount.
4. Let existing hook cleanup/reconnect lifecycle establish a fresh WebSocket session.

### M4 + L3 - ICU Placeholder Interpolation

1. Locate locale strings where placeholders were wrapped with single quotes.
2. Remove quote wrappers around `{device}` and `{title}` placeholders.
3. Keep wording unchanged otherwise to avoid translation drift.
4. Ensure both `vi` and `en` catalogs are updated in parity.

### M5 - Account Form Save Disable Logic

1. Normalize editable input values using `trim()`.
2. Compute `hasChanges` from username delta or non-empty email update.
3. Disable submit button when `saving` or no effective change exists.
4. Preserve backend validation as source of truth for uniqueness constraints.

## Files Changed

| File | Change Summary |
|------|---------------|
| `frontend/src/mocks/handlers/quizzes.handlers.ts` | Added status query filtering before pagination for admin quiz list |
| `frontend/src/components/features/quizzes/QuizFinishScreen.tsx` | Switched Try Again to restart nonce route replacement |
| `frontend/app/[locale]/(catalog)/quizzes/[id]/session/page.tsx` | Added restart searchParam handling and keyed remount |
| `frontend/src/components/features/profile/AccountForm.tsx` | Added normalized `hasChanges` and save button gating |
| `frontend/messages/vi.json` | Fixed ICU interpolation for `{title}` and `{device}` |
| `frontend/messages/en.json` | Fixed ICU interpolation for `{title}` and `{device}` |
| `docs/BUGS.md` | Moved H2/M2/M4/M5/L3 to Fixed section with fix notes |
| `docs/STATUS.md` | Added completed bugfix entry and report evidence link |
| `plan/refactor-quiz-profile-bugs-1.md` | Added deterministic implementation plan for bugfix batch |
| `docs/reports/2026-04-14_bugfix-h2-m2-m4-m5-l3.md` | Added this session report |

## Notes / Caveats

- Local file `.claude/settings.local.json` was intentionally excluded from commit scope.
- Validation executed: `npm run lint` and `npm run build` in frontend workspace.
- Remaining unresolved active items include H1/H3/M1/M3/M6/M7/L1/L2/L4 and are unaffected by this commit.
