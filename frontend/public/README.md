## `public/`

Static assets: images, icons, and MSW service worker registration.

Files:
- `mockServiceWorker.js` — MSW worker script (auto-generated by `npx msw init`)

**Rule**: Only commit truly static assets and MSW scripts. Avoid committing build outputs.
