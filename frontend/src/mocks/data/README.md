## `src/mocks/data/`

Shared mock data fixtures for realistic development testing.

Files:
- `fixtures.ts` — Mock users, courses, challenges, quizzes, etc. with realistic UUIDs and ISO dates

**Rule**: Fixtures are reused across multiple handlers to maintain data consistency during mocking.
